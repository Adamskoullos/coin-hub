import { Typography } from "@mui/material";

const Home = () => {
  return (
    <div>
      <Typography variant="h1" color="primary" sx={{ my: 20 }}>
        Landing Page
      </Typography>
    </div>
  );
};

export default Home;
